# Supabase Postgres example (pooled port 6543), be sure to require SSL
DATABASE_URL=postgresql+psycopg://postgres:<password>@<host>:6543/postgres?sslmode=require
JWT_SECRET=change-me
JWT_EXPIRE_MINUTES=60
